Optimisations d’import (import module → from module import nom)
Suppression de code mort
fusionné les boucles quand c est possibles
Suppression des sauts de ligne inutiles
Remplacement de la vectorisation par le module NumPy
Pré-calculer / pré-concaténer ce qui peut l’être
propagations des constantes
Remplacer les boucles simples par des compréhensions
Faire la concaténation avec "".join() ou des f-strings
Remplacer les print par des sys.stdout.write et conserver l'utilisations des parrametres flush, sep et end
regrouper les textes des nombreux print a la suite pour faire qu'un appelle
Remplacer les types quand nécessaire (list → tuple)
Élimination de conversions redondantes (int(float(x)) → int(x)).
Suppression de surcouche de typage (str("abc"))
inlining de petites fonctions
élimination de conditions triviales
Fusion ou simplification de blocs conditionnels.
Réduction des appels redondants (stockage des résultats d’expressions répétées).
remplacer des opérations lourdes par des équivalents moins coûteux (x**2 -> x*x)
Simplification d’expressions booléennes (a and True → a)
Ajouter des __slots__ pour les class
Supprimer le typage
remplacer les if ... is not None: par if ...: quand c est possible
passer de list.pop(index) -> del list[index] quand c est possible
remplacer le type bool par des int
faire en sorte qu'il ne prenne pas en charge les inputs

stop la compilation si el code ne s arrete pas ou si une erreur ou assert est rencontrer

convertir le code en .exe avec PyInstaller
ne pas supprimer les docstrings si l'options --doc est pas activer
suppressions des print|sys.stdout.write si l'options -GUI est activé
faire en sorte que le compilateur lance le programme apres la compilations

en .exe avec le logo, precompileravec le meme programme
PyBuilder 1.0